import { CheckCircle2, ShieldAlert } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { EvidenceBadge } from "@/components/app/evidence-badge";

export function PolicyApprovalPanel({ policy, hasProposal, approved, busy, onApprove }: { policy: { outcome: string; reasons: string[] } | null | undefined; hasProposal: boolean; approved: boolean; busy: string | null; onApprove: () => void }) {
  return <Card className="shadow-none"><CardHeader className="border-b"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-medium text-primary">03 · Deterministic authority</p><CardTitle className="mt-1">Policy and approval</CardTitle><CardDescription className="mt-1">Model text cannot bypass contact limits, promise protection, or human authority.</CardDescription></div><EvidenceBadge kind="measured">Deterministic</EvidenceBadge></div></CardHeader><CardContent>{policy ? <><div className={`rounded-xl border p-4 ${policy.outcome === "BLOCKED" ? "border-destructive/30 bg-destructive/5" : "border-amber-200 bg-amber-50"}`}><div className="flex items-center gap-2"><ShieldAlert className="size-4" /><span className="text-xs font-medium">{policy.outcome.replaceAll("_", " ")}</span></div><p className="mt-2 text-lg font-semibold">{policy.outcome === "APPROVAL_REQUIRED" ? "Human judgment required" : policy.outcome.replaceAll("_", " ")}</p></div><ul className="my-4 grid gap-2 text-xs text-muted-foreground">{policy.reasons.map((reason) => <li key={reason} className="flex gap-2"><span>•</span>{reason.replaceAll("_", " ")}</li>)}</ul>{!approved && hasProposal && policy.outcome !== "BLOCKED" ? <Button disabled={Boolean(busy)} onClick={onApprove}>{busy === "approve" ? "Recording…" : "Approve bounded action"}</Button> : approved ? <div className="flex items-center gap-2 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-xs font-medium text-emerald-800"><CheckCircle2 className="size-4" />Human approved · model output preserved</div> : <p className="text-xs text-destructive">Policy block · no handoff allowed</p>}</> : <p className="py-8 text-center text-sm text-muted-foreground">Policy runs after local schema and cross-field validation.</p>}</CardContent></Card>;
}
