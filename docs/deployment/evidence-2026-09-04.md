# Deployment evidence - 2026-09-04

Evidence boundary: this record covers local readiness, the deployed AWS origin, migration, Neon persistence across ECS replacement, public Cloudflare routing, the public judge journey, and one hosted Razorpay Test Mode partial payment verified by a signed public webhook. None of this is Live Mode money or evidence of real recovery uplift.

## Environment

- AWS account: `698268965353`
- AWS region: `ap-south-1`
- AWS CLI profile used: `recoup-deployer`
- CDK bootstrap stack: `arn:aws:cloudformation:ap-south-1:698268965353:stack/CDKToolkit/0bd7f6a0-a80d-11f1-bec3-0a872e4a6aeb`
- CDK bootstrap result: `CREATE_COMPLETE`
- ACM certificate: `arn:aws:acm:ap-south-1:698268965353:certificate/69225b83-7bf2-483e-8189-6ad30611171b`
- ACM status at capture: `ISSUED`
- Foundation stack: `arn:aws:cloudformation:ap-south-1:698268965353:stack/RecoupFoundation/fa702540-a811-11f1-97dd-067b6ae18d97`
- Foundation status: `CREATE_COMPLETE`
- Platform stack: `arn:aws:cloudformation:ap-south-1:698268965353:stack/RecoupPlatform/b562f6b0-a85e-11f1-a44d-0639f3ebcf17`
- Platform status at final snapshot: `UPDATE_COMPLETE`; CDK drift check reported zero drifted resources (six resource types unchecked by CloudFormation)
- ECR repository: `698268965353.dkr.ecr.ap-south-1.amazonaws.com/recoup`
- Deployed release tag: `release-20260904140106-scan`
- Deployed ECR image digest: `sha256:a64a6f14072bde96d30997a86ee68aab8d75e3f48a8f0fb938b23434767fb89c`
- ECR scan: `COMPLETE`; 2 critical, 7 high, and 1 medium findings were reported. All critical/high findings were in Alpine OpenSSL 3.5.7-r0, with no fixed package version reported by the scanner at capture time. This known demo risk was accepted because it does not block the hackathon feature path; do not reuse this acceptance for production.
- ALB DNS: `recoup-1279496202.ap-south-1.elb.amazonaws.com`
- ECS cluster/service: `recoup` / `recoup`
- Current web task definition: `recoup-web:6`
- Migration task definition: `recoup-migration:2`

## Verified locally

- ESLint: passed
- TypeScript: passed
- Non-database tests: 55 passed, 7 database-dependent tests skipped without `TEST_DATABASE_URL`
- Next.js production build: passed with standalone output
- CDK strict synth and cdk-nag: passed
- CDK foundation diff: reviewed; ECR repository and Secrets Manager configuration shell deployed successfully
- Production container: built for `linux/amd64`, runs as user `nextjs`, and returned `200` JSON from `/api/health`
- Graceful stop: SIGINT closed database connections and exited successfully
- Migration command: bundled in the production image and fails closed when `DATABASE_MIGRATION_URL` is absent
- Local image manifest: `sha256:307761f1c9bb41a1804610363eebcc575e560897c21030972bf703a58b1f93d9`
- Local image size: `79,868,812` bytes

## Verified in AWS

- The platform deploy completed and the service reached steady state with one desired/running task, zero pending tasks, and rollout state `COMPLETED`.
- The active ALB target was `healthy` on port 3000; direct origin HTTPS checks for `/api/health`, `/`, and `/collection` returned `200` with successful certificate verification for `recoup.aadithya.dev`.
- All three alarms were `OK`: `recoup-unhealthy-hosts`, `recoup-alb-5xx`, and `recoup-target-p99-latency`.
- The final migration/seed task was `arn:aws:ecs:ap-south-1:698268965353:task/recoup/9b1177863c7943a88b9569b621c6ab82`; it exited `0`. Log stream `migration/recoup-migration/9b1177863c7943a88b9569b621c6ab82` recorded successful migrations and bounded demo seeding.
- Seed verification found `INV-003` as case `rc_m7_inv_003`, open with ₹75,000 due, ₹0 recovered, `ACT_NOW`, and no payment recorded.
- Persistence was checked across a forced ECS replacement from task `41c628429a4c46f6aa43fed40501f782` to task `97d3c3a17cb641f899051d867dfbc1a5`. The canonical case response hash remained `7c7b2db9c828bf7762e71e899feb8dacb294dd007be84adc74fdad3a26583413` before and after replacement.
- A temporary task-definition revision used to recover the initial health-check configuration was deregistered. The final pre-acceptance drift check was clean; the UI correction was then deployed through CDK as `recoup-web:6`, and the service returned to a healthy completed rollout. A fresh post-rollout drift request could not be started after the AWS CLI session expired.
- Root cause of the initial ECS task recycling: the container health check addressed `127.0.0.1`, while Next.js bound to the ECS-provided hostname/task address. The checked-in health check now calls `http://$(hostname):3000/api/health`.
- Public `https://recoup.aadithya.dev/api/health` returned `200` through Cloudflare with successful TLS verification and `Cache-Control: no-store` / `CF-Cache-Status: DYNAMIC`.
- An unsigned POST to the public Razorpay webhook reached the application and returned `401` with `Invalid webhook signature`; this verifies routing while preserving raw-body signature enforcement. It is not payment or signed-webhook evidence.
- The SNS email subscription was confirmed as subscription `8d2c142c-a156-4174-96a1-84f51e1a41de`, but a later AWS status check reported it as `Deleted`; alert email delivery is therefore an optional outstanding repair.

## Hosted Razorpay Test Mode acceptance

- The live interpreter produced a grounded ₹40,000-now/remainder-on-Friday proposal with `DATA COLLECTION: DENY`; deterministic policy required approval, and the immutable proposal was approved before handoff.
- Partial-enabled Payment Link: `plink_TXyy18NdibJ9R0`; reference: `rec_ec9ccc515b_c0c88cd5eabf`; order: `order_TXyyIJDob5FxzE`.
- Captured Test Mode payment: `pay_TXyzlMlkJWaTKP`; method: netbanking; amount: `4000000` paise (₹40,000).
- Signed public event: `TXyzr0LJKPjwQ3`; type: `payment_link.partially_paid`; captured at `2026-09-04T13:52:18Z` and applied at `2026-09-04T13:52:24.100Z`.
- The application recorded one payment only, advanced `INV-003` from ₹75,000 to ₹35,000, set it to `PARTIALLY_PAID` / `WAIT_PROTECTED`, activated the authoritative ₹35,000 remainder promise, and promoted `INV-001` to `ACT_NOW`.
- Repeated reads after webhook delivery and another ECS replacement still reported exactly one payment and ₹40,000 recovered, providing hosted evidence for the idempotent transition.
- The public Command Center materialized the 96-case Recovery Frontier, decision queues, Decision Replay, and Evidence Lab. The collection page displayed the verified payment, protected remainder, queue reallocation, and full append-only timeline.
- The acceptance run exposed a stale “webhook pending” heading after partial payment. The condition was corrected to display `Partial payment verified; remainder protected`, verified locally, and deployed as `recoup-web:6`.

## Optional hardening

- Change Cloudflare's origin encryption mode from `Full` to `Full (strict)` so Cloudflare validates the already-issued ACM origin certificate. Plain `Full` currently works but does not validate the origin certificate.
- Recreate and reconfirm the deleted SNS email subscription if alarm emails are desired for the demo.
